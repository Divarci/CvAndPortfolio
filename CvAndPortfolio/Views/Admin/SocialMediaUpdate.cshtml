@model CvAndPortfolio.Models.Entity.TBLSOCIALMEDIA
@{
    ViewBag.Title = "SocialMediaUpdate";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<html>
<body>
    <div class="layout-page">
        <!-- Content wrapper -->
        <div class="content-wrapper">
            <!-- Content -->
            <div class="container-xxl flex-grow-1 container-p-y">
                <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">User Setings / Social Media /</span> Add</h4>
                <div class="row">
                    <div class="col-md-12">
                        <div class="card mb-4">
                            <h5 class="card-header">ADD NEW SOCIAL MEDIA</h5>
                            <hr class="my-0" />
                            <div class="card-body">
                                <form method="POST">
                                    <div class="row">
                                        <div class="mb-3 col-md-12">
                                            <label class="form-label">TITLE</label>
                                            <input class="form-control"
                                                   type="text"
                                                   name="TITLE"
                                                   value="@Model.TITLE"
                                                   autofocus
                                                   required />
                                        </div>
                                        <div class="mb-3 col-md-12">
                                            <label class="form-label">URL</label>
                                            <input type="text"
                                                   class="form-control"
                                                   name="URL"
                                                    value="@Model.URL"
                                                   required />
                                        </div>
                                        <div class="mb-3 col-md-6">
                                            <label class="form-label">ACLASS</label>
                                            <input type="text"
                                                   class="form-control"
                                                   name="ACLASS"
                                                    value="@Model.ACLASS"
                                                   required />
                                        </div>
                                        <div class="mb-3 col-md-6">
                                            <label class="form-label">ICLASS</label>
                                            <input type="text"
                                                   class="form-control"
                                                   name="ICLASS"
                                                    value="@Model.ICLASS"
                                                   required />
                                        </div>
                                    </div>
                                    <div class="mt-2">
                                        <button type="submit" class="btn btn-primary me-2">Update</button>
                                        <button type="reset" class="btn btn-outline-secondary">Cancel</button>
                                    </div>
                                </form>
                            </div>

                        </div>

                    </div>
                </div>
            </div>
        </div>

    </div>
</body>
</html>
